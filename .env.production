# Copy to .env.production at repo root for backend production config
# Then set APP_ENV=production when running the backend to prefer this file.

# Supabase (backend uses service role or anon depending on needs)
SUPABASE_URL=https://jjmazcflqjculvzwoshy.supabase.co
SUPABASE_KEY=sb_secret_2ppt1OymfK4mYh1am4Jhag_cJF_c-b4
# Prefer JWKS for token verification (new default in Supabase). Optional if you verify elsewhere.
SUPABASE_JWKS_URL=https://jjmazcflqjculvzwoshy.supabase.co/auth/v1/.well-known/jwks.json
# If you still use HS256 tokens, you can optionally set the legacy shared secret instead of JWKS
# SUPABASE_JWT_SECRET=your_legacy_hs256_secret

# CORS/frontend URL(s)
ALLOWED_ORIGINS= http://localhost:5173/
FRONTEND_BASE_URL= http://localhost:5173/

# App
APP_ENV=production
PORT=8000
